// ------------------------------------------------------------------
//  Shared presentational types for LifeOS v2.
//  Domain/data types will live with their features (Supabase) later.
// ------------------------------------------------------------------

/** Severity drives the only places where colour signals urgency. */
export type Severity = "danger" | "warn" | "info";
