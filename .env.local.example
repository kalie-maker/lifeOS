# LifeOS v2 — variables de entorno
#
# Copie este archivo a .env.local y rellene su clave.
# .env.local NO se sube al repositorio (está en .gitignore).

# Clave de la API de Anthropic (Claude). Se usa SOLO en el servidor,
# en /api/capture. Nunca se expone al cliente.
ANTHROPIC_API_KEY=sk-ant-...
